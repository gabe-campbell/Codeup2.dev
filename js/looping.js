$('li em').addClass('seasonal');
$('li.hot').addClass('favorite');

// This exercise first goes through all list items with an em tag and adds the class seasonal to all matching elements, applying new CSS rules
// The second line goes through each list item and selects the ones with a class of hot and adds the class favorite to it, applying new CSS rules
