// Select the element and store it in a variable
var el = document.getElementById('one');

// Change the value of the class attribute
el.className = 'cool';

// This exercise shows how to access an element by its id attribute and sets it to a variable, it then uses the DOM to change add the class name of the element
