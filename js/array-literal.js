// Create an array and assign it values

var colors;
colors = ['white', 'black', 'custom'];

// Show the first item from the array

var el = document.getElementById('colors');
el.textContent = colors[0];


// This exercise shows how to create a variable and set it to an array of colors and then to display one of those colors using the array index
